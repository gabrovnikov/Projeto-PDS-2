#include <iostream>
#include <string>
#include <ctime>
#include <cstdlib>
#include <chrono>
#include <thread>
#include <string>
#include <cmath>

#include "historico.hpp"
#include "corrida.hpp"
#include "realizar_o_cadastro.hpp"

  using namespace std;

int main() {
  cout << "Hello World!\n";
  return 0;
}



# Uber
  O código tem como objetivo simular o funcionamento de um aplicativo de uber.
Nele, o usuário(passageiro) pode Pedir_Corridas, confirmar_corrida, estatos_da_corrida,avaliar_motorista e ver valor_da_corrida. Do mesmo modo, o motorista pode confirmar_corrida, avaliar_passageiro, ver valor_da_corrida
  Inicialmente, se faz um cadastro dentro do aplicativo, indicando a função de passageiro ou condutor. A partir do momento da simulação, o código gera distância e valor da corrida. Logo em seguida, deve-se haver um match mutuo que representa que o passageiro e condutor aceitaram aquela corrida em questão. Assim, o programa apresenta que o motorista está a caminho do passageiro, e então a viagem entra em andamento. Após a distância inicialmente gerada for cumprida e o condutor confirmar o pagamento, é possível que os dois usuários avaliem um ao outro. Também, é possível que esses usuários visualizem seus respectivos históricos. 
  Sendo assim, encontramos bastante dificuldade em finalizar o projeto, especialmente na parte de modularização do código. É importante ressaltar que todos os integrantes colaboraram com o projeto, fizemos reuniões praticamente toda semana através do "Discord" e o site "replit.com".






  # Uber
  O código tem como objetivo simular o funcionamento de um aplicativo de uber.
  Inicialmente, se faz um cadastro dentro do aplicativo, indicando a função de passageiro ou condutor. A partir do momento da simulação, o código gera distância e valor da corrida. Logo em seguida, deve-se haver um match mutuo que representa que o passageiro e condutor aceitaram aquela corrida em questão. Assim, o programa apresenta que o motorista está a caminho do passageiro, e então a viagem entra em andamento. Após a distância inicialmente gerada for cumprida e o condutor confirmar o pagamento, é possível que os dois usuários avaliem um ao outro. Também, é possível que esses usuários visualizem seus respectivos históricos. 
  Sendo assim, encontramos bastante dificuldade em finalizar o projeto, especialmente na parte de modularização do código. É importante ressaltar que todos os integrantes colaboraram com o projeto, fizemos reuniões praticamente toda semana através do "Discord" e o site "replit.com".
